services:
  kimi2api:
    build:
      context: .
      dockerfile: Dockerfile
    image: kimi2api:latest
    container_name: kimi2api
    ports:
      - "8080:8080"
    volumes:
      - ./config.json:/data/config.json
    restart: unless-stopped